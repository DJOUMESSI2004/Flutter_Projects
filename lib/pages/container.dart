import 'package:flutter/material.dart';

class BoxNotions extends StatelessWidget {
  const BoxNotions({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      padding: const EdgeInsets.all(30),
    );
  }
}